[package]
name = "ffi-cxx-spike"
version = "0.1.0"
edition = "2021"

# M0 spike（D28）：approach B = cxx。独立于 workspace（不污染主工程），
# 构建：cargo build --manifest-path spike/cxx/Cargo.toml

[dependencies]
cxx = "1"

[build-dependencies]
cxx-build = "1"